import { Component, PropTypes } from "react";

export default class DataTable extends Component {
    static propTypes = {
      
    }
    render() {
      return <div> this is datatable dum</div>;
    }
}